const name_input = document.getElementById('name');
const price_input = document.getElementById('price');
const weight_input = document.getElementById('weight');
const stock_r_input = document.getElementById('rakuten_stock');
const stock_m_input = document.getElementById('makeshop_stock');
const jancode_input = document.getElementById('jancode');

const descriptions = () => document.getElementById('descriptsions').querySelectorAll('div');
const details = () => document.getElementById('details').querySelectorAll('div');

// save input
document.getElementById('save').addEventListener('click', () => {
  return;
})

// registor new item
document.getElementById('new').addEventListener('click', () => {
  return;
})

// delete this item
document.getElementById('delete').addEventListener('click', () => {
  return;
})
